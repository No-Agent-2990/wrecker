// Network credentials and IP

// STA defines used if in station mode. This would be the SSID and PWD of the network to connect *to*.
#define STA_HOSTAP_SSID   "HOSTAPSSID"
#define STA_HOSTAP_PWD    "password"

// AP defines used if acting as AP. This is the SSID and PWD to connect to this unit.
#define AP_SSID           "APSSID"
#define AP_PWD            "appassword"
#define AP_IPADDR         IPAddress(192,168,1,100)
#define AP_NETMASK        IPAddress(255,255,255,0)
